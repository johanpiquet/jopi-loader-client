{
  "name": "@jopi-loader/client",
  "version": "1.1.45",
  "private": false,
  "license": "MIT",
  "keywords": ["loader"],
  "description": "Client library for jopi-loader",

  "engines": {
    "bun": "^1.2.18",
    "node": ">=22.18.0"
  },

  "type": "module",
  "main": "./index.js",
  "types": "./index.d.ts",

  "scripts": {
    "tsc": "npx tsc",
    "bun-publish": "npx tsc && bun publish",
    "clean": "rm -rf ./dist; rm -rf .turbo; rm tsconfig.tsbuildinfo"
  },

  "devDependencies": {
    "@types/bun": "latest"
  },

  "dependencies": {
    "jopi-node-space": "workspace:^"
  }
}